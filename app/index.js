import { Text, View } from "react-native";
import { useState } from "react";
import { useRouter, Stack } from "expo-router";
const Home = () => {
  const router = useRouter();

  return (
    <View>
      <Text>Hello world! Ws</Text>
    </View>
  );
};

export default Home;
